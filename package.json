{
  "name": "s3lambda",
  "version": "1.0.0",
  "description": "AWS Lambda to convert new files to zip files.",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Carter J. Cobb",
  "license": "ISC",
  "dependencies": {
    "archiver": "^5.3.0",
    "aws-sdk": "^2.1095.0",
    "stream": "^0.0.2"
  }
}
